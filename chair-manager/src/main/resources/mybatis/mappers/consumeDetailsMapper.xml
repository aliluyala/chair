<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.chair.manager.mapper.ConsumedDetailsMapper">

	<select id="queryConsumedDetailsList" resultType="ConsumedDetails">
		<!-- SELECT * FROM consumed_details where open_id=#{openId} and phone_number=#{phoneNumber} ORDER BY last_update DESC; -->
		SELECT * FROM consumed_details where open_id=#{openId} ORDER BY last_update DESC;
	</select>
	
</mapper>