<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.chair.manager.mapper.ConsumePackageMapper">

	<select id="queryListByLimit" resultType="ConsumePackage">
		SELECT * FROM consumed_package ORDER BY create_time DESC limit 4
	</select>
	
</mapper>